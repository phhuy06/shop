import styles from './Footer.module.scss';
function Footer() {
    return (
        <footer className={styles.container}>
            <h2>footer</h2>
        </footer>
    );
}

export default Footer;
